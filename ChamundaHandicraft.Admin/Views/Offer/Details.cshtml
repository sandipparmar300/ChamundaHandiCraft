@model OfferGridItem
@{
    ViewData["Title"] = "Offer Details";
}

@section Header {
    <div class="page-head commondetails-page-cls">
        <div class="page-head-left">
            <a asp-controller="Offer" asp-action="Index">
                <picture><img src="~/assets/images/small-left-arrow.svg" alt="back" width="18" height="18" /></picture>
            </a>
            <h2 class="page-title">Offer Details</h2>
        </div>
        <ul class="page-breadcrumb">
            <li><a asp-controller="Offer" asp-action="Index" class="links">Offers Listing</a></li>
            <li>Details</li>
        </ul>
    </div>
}

<main class="page-content">

    <section class="form-section">
        <div class="form-section-header d-flex justify-content-between align-items-center">
            <h2><i class="fas fa-circle-info" style="color:var(--accent);margin-right:8px;"></i>Offer Information</h2>
            <a asp-controller="Offer" asp-action="Edit" asp-route-id="@Model.Id" class="custom_button secondary_button"><i class="fas fa-pen"></i> Edit</a>
        </div>

        <div class="form-section-body">
            <div class="row g-3">
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Offer Name</label>
                        <p class="detail-value">@Model.Name</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Offer Type</label>
                        <p class="detail-value">@Model.OfferType</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Discount Value</label>
                        <p class="detail-value">@Model.DiscountPercent%</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Applies To</label>
                        <p class="detail-value">@Model.ProductCount products</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Priority</label>
                        <p class="detail-value">&mdash;</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Valid From</label>
                        <p class="detail-value">@Money.DateAndTime(Model.StartsOn)</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Valid To</label>
                        <p class="detail-value">@Money.DateAndTime(Model.EndsOn)</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Show Countdown</label>
                        <p class="detail-value">@(Model.IsLiveNow ? "Yes — anchored to the end time above" : "No")</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Stackable</label>
                        <p class="detail-value">&mdash;</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Orders Using Offer</label>
                        <p class="detail-value">@Model.OrdersInfluenced</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Status</label>
                        <p class="detail-value">@(Model.IsLiveNow ? "Live now" : Model.IsActive ? "Scheduled" : "Inactive")</p>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <label class="form-label-custom">Created By</label>
                        <p class="detail-value">@(Model.CreatedBy ?? "—")</p>
                    </div>
            </div>
        </div>
    </section>

</main>

@section Scripts {
    <script>
        $(document).ready(function () {
            if (typeof fnSelectMenu === 'function') { fnSelectMenu("menu-Promotions"); }
        });
    </script>
}
